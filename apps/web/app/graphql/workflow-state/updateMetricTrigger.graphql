mutation UpdateMetricTrigger(
  $metricType: MetricType!
  $workflowStateId: String!
) {
  updateMetricTrigger(
    metricType: $metricType
    workflowStateId: $workflowStateId
  ) {
    ...WorkflowState
  }
}
