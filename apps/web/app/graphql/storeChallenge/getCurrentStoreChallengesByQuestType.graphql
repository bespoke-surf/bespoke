query GetCurrentStoreChallengeByQuestType(
  $subdomain: String!
  $questType: QuestType!
) {
  getCurrentStoreChallengesByQuestType(
    subdomain: $subdomain
    questType: $questType
  ) {
    ...StoreChallenge
  }
}
